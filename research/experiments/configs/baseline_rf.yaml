# TF-IDF + Random Forest baseline (Phases.md Batch J).
# Real, executable model -- see propagate_research/models/baselines.py.
model_type: baseline_rf
dataset: politifact
seed: 42
hyperparameters:
  max_features: 20000
  ngram_range: [1, 2]
  n_estimators: 200
checkpoint_dir: model_checkpoints/baseline_rf
output_dir: evaluation
