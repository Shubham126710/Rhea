"""
Auth module.

Responsibility (Architecture.md §2, Master Build §6.2 "Auth"):
signup, login, session issuance/validation, password reset, account
deletion, RBAC role checks.

Not implemented yet — this module is built out in Phase 1
(Phases.md / Master Build §25 Phase 1). This package exists now only
so the module boundary is real from Phase 0 onward, per Rules.md §1
(no ghost folders — but also no code claiming to do something it
doesn't do yet).
"""
