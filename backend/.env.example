ENVIRONMENT=development
DATABASE_URL=postgresql+psycopg://propagate:propagate@localhost:5432/propagate
REDIS_URL=redis://localhost:6379/0
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Phase 2 (Shared Preprocessing) ---
# All of these have working defaults (see app/core/config.py) — only
# set them here if you need to override for your environment.
PREPROCESSING_VERSION=1.0.0
PREPROCESSING_MAX_UPLOAD_BYTES=10485760
PREPROCESSING_URL_FETCH_TIMEOUT_SECONDS=10.0
PREPROCESSING_URL_FETCH_MAX_REDIRECTS=5
PREPROCESSING_URL_FETCH_MAX_BYTES=10485760
PREPROCESSING_MAX_EXTRACTED_TEXT_CHARS=2000000
